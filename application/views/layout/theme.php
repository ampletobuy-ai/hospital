<?php
// AdminLTE theme CSS removed — now using sh-tokens variant system (variant-a/b/c body class).
// Old CSS files renamed to *_old.css in backend/dist/themes/ for reference; delete after confirming no regressions.
$rtl = $this->customlib->getRTL();
